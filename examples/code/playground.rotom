// This is a playground file, write your crap here
fn sum(a, b) -> a + b

struct Person {
    firstName: string,
    lastName: string,
    
    fullName: { firstName + " " + lastName }
};

fn main() {
    let person = Person(
        firstName="Samuel",
        lastName="David",
    );

    print person.fullName();
}
